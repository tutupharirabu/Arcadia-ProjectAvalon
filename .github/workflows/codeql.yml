name: CodeQL

on:
  push:
    branches: [main, canary, dev]
  pull_request:
    branches: [main, canary, dev]
  schedule:
    - cron: "0 3 * * 1" # Senin 03:00 UTC — scan rutin mingguan

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions:
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [javascript-typescript, php]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup PHP (untuk autobuild composer)
        if: matrix.language == 'php'
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_sqlite, sqlite3, fileinfo, bcmath
          coverage: none

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
